# Web App для календаря

Это мини-приложение (Web App) для Telegram бота с функционалом календаря и рисования.

## Размещение

Для работы Web App необходимо разместить файл `index.html` на веб-сервере с HTTPS.

### Варианты размещения:

1. **GitHub Pages** (бесплатно):
   - Создайте репозиторий на GitHub
   - Загрузите файл `index.html`
   - Включите GitHub Pages в настройках репозитория
   - URL будет: `https://yourusername.github.io/repository/index.html`

2. **Netlify/Vercel** (бесплатно):
   - Загрузите файл через веб-интерфейс
   - Получите автоматический HTTPS URL

3. **Свой сервер**:
   - Разместите файл на вашем веб-сервере
   - Убедитесь, что используется HTTPS

4. **Для локальной разработки**:
   - Используйте `ngrok` или `localtunnel` для создания HTTPS туннеля
   - Пример с ngrok: `ngrok http 8000`
   - Затем разместите файл на локальном сервере (например, `python -m http.server 8000`)

## Настройка

После размещения файла обновите `WEB_APP_URL` в файле `config.py`:

```python
WEB_APP_URL = "https://your-domain.com/webapp/index.html"
```

## Функционал

- 📅 Календарь с навигацией по месяцам
- ✏️ Режим карандаша для добавления пометок
- 🧹 Режим ластика для удаления пометок
- 💾 Сохранение изменений в бот
- 📱 Адаптивный дизайн для мобильных устройств

## Технические детали

Web App использует Telegram Web App API для:
- Получения информации о пользователе
- Отправки данных обратно в бот
- Интеграции с темой Telegram

